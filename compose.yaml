services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=educ_manager'
      - 'POSTGRES_PASSWORD=mypassword'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
